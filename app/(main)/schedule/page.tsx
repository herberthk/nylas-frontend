import '@/components/schedule/styles/schedule-editor.scss';

import React from 'react';

import Schedule from '@/components/schedule/Schedule';

const page = (): React.JSX.Element => {
  return <Schedule />;
};

export default page;
